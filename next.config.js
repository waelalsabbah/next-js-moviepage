/** @type {import('next').NextConfig} */
const nextConfig = {
  VideoPlayer: {
    domains: ['video.youtube.com'],
  },
};

module.exports = nextConfig;

/* const nextConfig = {
  experimental: {
    typedRoutes: true,
  },
};

module.exports = nextConfig; */
